[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.build]
cmds = ["php bin/console cache:clear --env=prod --no-warmup"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"